# MSSQL Server Configuration
# For default instance: MSSQL_SERVER=localhost
# For named instance: MSSQL_SERVER=localhost\SQLEXPRESS
MSSQL_SERVER=localhost
MSSQL_DATABASE=your_database_name

# Authentication
# SQL Authentication (required - Windows Authentication not supported)
MSSQL_USER=your_sql_username
MSSQL_PASSWORD=your_sql_password

# IMPORTANT: Windows Authentication (MSSQL_TRUSTED_CONNECTION=true) is NOT supported
# The mssql package's default tedious driver cannot use integrated Windows Authentication.
# To use SQL Authentication with Windows/domain accounts, you must:
# 1. Enable SQL Server Mixed Mode Authentication in SQL Server
# 2. Create a SQL Server login with username/password
# Example for local development with SQL Express:
#   MSSQL_SERVER=localhost\SQLEXPRESS
#   MSSQL_USER=sa (or create a specific user)
#   MSSQL_PASSWORD=your_password

# SSL/TLS Configuration
# For local/development servers:
MSSQL_ENCRYPT=false
MSSQL_TRUST_CERT=true

# For production servers with valid SSL certificates:
# MSSQL_ENCRYPT=true
# MSSQL_TRUST_CERT=false